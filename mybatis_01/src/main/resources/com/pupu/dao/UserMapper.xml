<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- dao的全限定类名 -->
<mapper namespace="com.pupu.com.pupu.dao.UserDao">
    <select id="findAll" resultType="com.pupu.domain.User">
        <!-- mysql表名区分大小写 -->
        select * from USER
    </select>
</mapper>